# Meeting Minutes Summarizer Dependencies

# Core dependencies
typing-extensions>=4.5.0  # For enhanced type hints
dataclasses>=0.8; python_version < '3.7'  # Backport for older Python versions

# Development dependencies (optional)
pytest>=7.0.0  # For testing
black>=22.0.0  # For code formatting
flake8>=4.0.0  # For linting
mypy>=0.991  # For type checking

# Future dependencies (for when integrating with Claude API)
# anthropic>=0.5.0  # Claude API client
# requests>=2.28.0  # HTTP requests
# python-dotenv>=0.19.0  # Environment variable management